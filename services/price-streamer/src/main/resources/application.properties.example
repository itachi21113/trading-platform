# Run this service on port 8081 to avoid conflict with the producer's port 8080
server.port=8081

# --- Kafka Connection Settings ---

# 1. Tell Spring Boot where to find the Kafka server
spring.kafka.bootstrap-servers=localhost:9092

# --- Consumer-Specific Settings ---

# 2. Create a group ID for our consumer. All instances of this service will share this ID.
spring.kafka.consumer.group-id=price-streamer-group

# 3. Tell Spring how to convert the message key from text
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer

# 4. Tell Spring how to convert the JSON message from Kafka back into our Java PriceTick object
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=*